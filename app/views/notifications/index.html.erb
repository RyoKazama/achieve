<div class="container">
  <div class="wrapper col-md-8 col-md-offset-2 col-sm-10">

    <h2>お知らせ</h2>
    <ul>
      <% @notifications.each do |notification| %>
        <div class="row">
            <button class="btn btn-default" disabled="disabled"><% notification.read %>未読</button>
            <p>
            <%= notification.comment.user.try(:name) %>さんが
            あなたのブログ<strong>(<%= link_to "#{notification.comment.blog.title}", blog_path(notification.comment.blog, notification_id:notification.id) %>)</strong>にコメントしました。
            </p>
          <p><%= notification.created_at.strftime('%m月%d日')  %></p>
       </div>
      <% end %>
    </ul>

  </div>
</div>
